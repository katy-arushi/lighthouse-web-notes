{
  "name": "sql-from-our-apps",
  "version": "1.0.0",
  "description": "- [] Review the online library database\r - [] Perform `BREAD` or `CRUD` actions on database via command line app\r - [] How to avoid SQL injection attack\r - [] Serve database content to the browser(client)\r - [] Protect secrets with Environment Variables",
  "main": "command-line.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1"
  }
}
